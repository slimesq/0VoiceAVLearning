# Documentation

This directory contains maintenance notes that are useful for the project but
too detailed for the root README.

- `ffmpeg-dependency.md`: FFmpeg flavor selection, Conan option mapping, and
  native FFmpeg configure flags that the Conan Center recipe does not expose.
